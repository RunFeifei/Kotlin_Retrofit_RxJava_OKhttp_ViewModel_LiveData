<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg"
    tools:context=".ui.LoginActivity">

    <ImageView
        android:id="@+id/imgSign1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="200dp"
        android:layout_marginRight="2dp"
        android:src="@drawable/sign1"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@+id/edtName"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/edtName"
        android:layout_width="200dp"
        android:layout_height="30dp"
        android:layout_marginLeft="2dp"
        android:textColor="#ffffff"
        android:background="@color/colorPrimary"
        app:layout_constraintBottom_toBottomOf="@id/imgSign1"
        app:layout_constraintLeft_toRightOf="@id/imgSign1"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/imgSign1"
        tools:background="#00ff00" />

    <ImageView
        android:id="@+id/imgSign2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:src="@drawable/sign2"
        app:layout_constraintLeft_toLeftOf="@id/imgSign1"
        app:layout_constraintTop_toBottomOf="@id/imgSign1" />

    <EditText
        android:id="@+id/edtPass"
        android:layout_width="200dp"
        android:layout_height="30dp"
        android:textColor="#ffffff"
        android:background="@color/colorPrimary"
        app:layout_constraintBottom_toBottomOf="@id/imgSign2"
        app:layout_constraintLeft_toLeftOf="@id/edtName"
        app:layout_constraintTop_toTopOf="@id/imgSign2"
        tools:background="#00ff00" />

    <TextView
        android:id="@+id/textLogin"
        android:layout_width="210dp"
        android:layout_height="40dp"
        android:layout_marginTop="30dp"
        android:background="#0F6FB6"
        android:gravity="center"
        android:text="登录"
        android:textColor="#ffffff"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imgSign2" />

</androidx.constraintlayout.widget.ConstraintLayout>